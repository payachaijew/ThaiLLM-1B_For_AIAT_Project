# Phase 0 -- tokenizer screen + baseline BPB. CPU/MPS is enough for Steps A and B.
torch>=2.4
transformers>=4.51        # Qwen3 needs >=4.51; gemma4 needs a newer release (see README)
tokenizers>=0.20
huggingface_hub>=0.30
pyarrow>=21.0
sentencepiece
protobuf
# Step C downstream accuracy (pin the commit and record it in every run record):
# git+https://github.com/EleutherAI/lm-evaluation-harness@<COMMIT_SHA>
